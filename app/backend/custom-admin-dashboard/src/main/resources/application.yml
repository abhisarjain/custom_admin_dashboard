server:
  port: ${PORT:8080}

spring:
  application:
    name: custom-admin-dashboard

  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration

  datasource:
    url: jdbc:postgresql://ep-polished-leaf-ama3qcng.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&channelBinding=require
    username: neondb_owner
    password: npg_IM4hFeV2jnmx
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false

  security:
    user:
      password: disabled

jasypt:
  encryptor:
    password: myJasyptEncryptionKey2024AdminDashboard

jwt:
  secret: mySecretJwtKey2024ForAdminDashboardPlatformApp
  expiration: 86400000

logging:
  level:
    com.customadmindashboard: INFO

